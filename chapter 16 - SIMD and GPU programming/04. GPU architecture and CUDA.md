# Архитектура графического процессора и CUDA

*Графические процессоры изменили ИИ, предоставив тысячи ядер для массового параллелизма. В этом файле описываются философия проектирования графического процессора и процессора, иерархия памяти графического процессора, программирование CUDA на C++, модель выполнения SIMT, шаблоны доступа к памяти, синхронизация, потоки, профилирование и поколения графических процессоров NVIDIA, а также знания, необходимые для написания и понимания ядер графического процессора.*

- Практические руководства по CUDA с полными рабочими примерами см. в сопутствующем репозитории: [github.com/HenryNdubuaku/cuda-tutorials](https://github.com/HenryNdubuaku/cuda-tutorials).

- Современный графический процессор NVIDIA имеет более 10 000 ядер CUDA. Процессор имеет от 4 до 128 ядер. Это основное преимущество в 100–1000 раз является причиной того, что графические процессоры доминируют в машинном обучении: обучение преобразователя требует триллионов операций умножения-сложения, а графические процессоры обрабатывают их параллельно в масштабе, с которым не могут сравниться центральные процессоры.

- Даже если вы никогда не пишете ядра CUDA самостоятельно, понимание архитектуры графического процессора объясняет: почему размер пакета имеет значение (необходимо достаточно работы для насыщения графического процессора), почему память обычно является узким местом (а не вычислениями) и почему некоторые операции (разброс, условное ветвление) выполняются на графических процессорах медленно.

## Графический процессор и ЦП: принципиально разные конструкции

- ЦП рассчитан на **задержку**: минимизируйте время выполнения одной задачи. Большую часть своего транзисторного бюджета он тратит на кэширование, предсказатели ветвей и выполнение вне очереди — все это для ускорения одного потока.

- Графический процессор рассчитан на **пропускную способность**: максимизируйте количество задач, выполняемых в секунду. Он отводит большую часть транзисторов исполнительным устройствам (ALU). Отдельные потоки работают медленно, но их тысячи.

| | процессор | графический процессор |
|--|-----|-----|
| Ядра | 4-128 (сложный, быстрый) | 1000-20000 (простой, медленный) |
| Тактовая скорость | 3–5 ГГц | 1–2,5 ГГц |
| Кэш | Большой (32 МБ+ L3) | Малый (общая память для каждого SM) |
| Предсказание отрасли | Сложный | Нет (все потоки следуют по одному и тому же пути) |
| Лучшее для | Сложный поток управления с малой задержкой | Высокая пропускная способность, параллельная работа с данными |
| Типичные FLOPS (FP32) | 1-5 терафлопс | 30-80 терафлопс |
| Пропускная способность памяти | 50–100 ГБ/с | 1–3 ТБ/с |

- Преимущество пропускной способности памяти графического процессора (в 10–30 раз) часто более важно, чем его вычислительное преимущество. Многие операции машинного обучения привязаны к памяти (поэлементные операции, нормализация, внимание), а пропускная способность графического процессора позволяет ему достаточно быстро передавать данные в свои ядра.

## Иерархия памяти графического процессора

- Понимание памяти графического процессора имеет решающее значение, поскольку **основным узким местом является доступ к памяти**, а не вычисления.

| Память | Размер | Задержка | Пропускная способность | Область применения |
|--------|------|---------|-----------|-------|
| Регистры | ~256 КБ на SM | 0 циклов | Самый высокий | За тему |
| Общая память | 48-228 КБ на SM | ~5 циклов | ~20 ТБ/с | За блок потока |
| Кэш L1 | 128-256 КБ на SM | ~30 циклов | | Согласно СМ |
| Кэш L2 | 4–96 МБ | ~200 циклов | ~6 ТБ/с | Глобальный |
| Глобальная память (HBM) | 24–192 ГБ | ~400 циклов | 1–3,3 ТБ/с | Глобальный |

- **Регистры** являются самыми быстрыми, но наиболее ограниченными. Каждый поток имеет собственный набор регистров (обычно максимум 255). Использование слишком большого количества регистров на поток снижает **заполняемость** (меньше потоков может работать одновременно).

- **Общая память** — это управляемый программистом кеш, общий для всех потоков в блоке. Это ключ к написанию быстрых ядер CUDA: загрузите фрагмент данных из медленной глобальной памяти в быструю разделяемую память, а затем выполните на ней вычисления. Это **паттерн тайлинга**, который доминирует в программировании на графических процессорах.

- **Глобальная память (HBM)**: основная память графического процессора (VRAM). Большой, но медленный (задержка 400 циклов). Все данные начинаются и заканчиваются здесь. Цель оптимизации ядра — минимизировать доступ к глобальной памяти.

## Модель программирования CUDA

- CUDA (Compute Unified Device Architecture) — это модель программирования NVIDIA для графических процессоров. Вы пишете **ядра**: функции, которые выполняются на графическом процессоре и выполняются тысячами потоков одновременно.

### Иерархия: сетки, блоки, потоки

```
Grid (the entire launch)
├── Block (0,0)
│   ├── Thread (0,0)
│   ├── Thread (1,0)
│   ├── Thread (2,0)
│   └── ... (up to 1024 threads per block)
├── Block (1,0)
│   ├── Thread (0,0)
│   └── ...
└── ... (millions of blocks possible)
```

- **Резьба**: наименьшая единица. Каждый поток имеет уникальный идентификатор (`threadIdx.x`) внутри своего блока.
- **Блок**: группа потоков, которые могут совместно использовать память и синхронизироваться. Идентификатор блока: `blockIdx.x`. Размер блока: `blockDim.x` (до 1024 потоков).
- **Сетка**: все блоки, запускаемые одним ядром. Может быть 1D, 2D или 3D.

- Каждый поток вычисляет свой глобальный индекс: `int idx = blockIdx.x * blockDim.x + threadIdx.x;`.

### Ваше первое ядро CUDA

```cpp
// vector_add.cu — CUDA source file (.cu extension)

#include <stdio.h>

// __global__ marks this as a GPU kernel (called from CPU, runs on GPU)
__global__ void vector_add(const float* a, const float* b, float* c, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    if (idx < n) {           // bounds check (grid may be larger than data)
        c[idx] = a[idx] + b[idx];
    }
}

int main() {
    int n = 1 << 20;  // ~1 million elements
    size_t bytes = n * sizeof(float);

    // Allocate host (CPU) memory
    float *h_a = new float[n];
    float *h_b = new float[n];
    float *h_c = new float[n];

    // Initialise
    for (int i = 0; i < n; i++) {
        h_a[i] = 1.0f;
        h_b[i] = 2.0f;
    }

    // Allocate device (GPU) memory
    float *d_a, *d_b, *d_c;
    cudaMalloc(&d_a, bytes);
    cudaMalloc(&d_b, bytes);
    cudaMalloc(&d_c, bytes);

    // Copy data from CPU to GPU
    cudaMemcpy(d_a, h_a, bytes, cudaMemcpyHostToDevice);
    cudaMemcpy(d_b, h_b, bytes, cudaMemcpyHostToDevice);

    // Launch kernel: 256 threads per block, enough blocks to cover n elements
    int block_size = 256;
    int grid_size = (n + block_size - 1) / block_size;  // ceiling division
    vector_add<<<grid_size, block_size>>>(d_a, d_b, d_c, n);

    // Copy result from GPU to CPU
    cudaMemcpy(h_c, d_a, bytes, cudaMemcpyDeviceToHost);

    // Verify
    printf("c[0] = %f (expected 3.0)\n", h_c[0]);

    // Free memory
    cudaFree(d_a); cudaFree(d_b); cudaFree(d_c);
    delete[] h_a; delete[] h_b; delete[] h_c;

    return 0;
}
``````bash
# Compile with NVIDIA's compiler
nvcc -O3 -o vector_add vector_add.cu
./vector_add
```

- **Ключевые концепции C++ в CUDA**:
    - `__global__`: ключевое слово CUDA, обозначающее функцию ядра. Вызывается из ЦП (`host`), работает на графическом процессоре (`device`).
    - `<<<grid_size, block_size>>>`: синтаксис запуска ядра. Указывает, сколько блоков и потоков использовать.
    - `cudaMalloc` / `cudaFree`: выделить/освободить память графического процессора (как `new`/`delete`, но для графического процессора).
    - `cudaMemcpy`: копирование данных между процессором и графическим процессором. Зачастую это самое большое узкое место (пропускная способность PCIe составляет ~32 ГБ/с, а пропускная способность памяти графического процессора — ~3 ТБ/с).

### Варпы и SIMT

- Графический процессор выполняет потоки группами по 32, называемыми **варпами**. Все 32 потока в варпе одновременно выполняют **одну и ту же инструкцию** (одна инструкция, несколько потоков — SIMT). Это эквивалент SIMD для графического процессора, но на уровне потока.

- **Расхождение варпов** возникает, когда потоки в одной и той же варпе берут разные ветви оператора `if`. Графический процессор не может одновременно выполнять две разные инструкции в одном варпе, поэтому он выполняет обе ветви последовательно, маскируя потоки, которые не должны участвовать. Это снижает производительность вдвое (или хуже).

```cpp
// BAD: warp divergence (threads in same warp take different paths)
if (threadIdx.x % 2 == 0) {
    c[idx] = a[idx] + b[idx];    // even threads do this
} else {
    c[idx] = a[idx] - b[idx];    // odd threads do this (same warp, serialised)
}

// BETTER: branchless (no divergence)
float sign = (threadIdx.x % 2 == 0) ? 1.0f : -1.0f;
c[idx] = a[idx] + sign * b[idx];  // all threads execute the same instruction
```

### Объединение памяти

- **Объединенный доступ**: когда последовательные потоки обращаются к последовательным адресам памяти, графический процессор объединяет их в одну транзакцию памяти. Это критично для производительности.

```cpp
// GOOD: coalesced — thread 0 reads a[0], thread 1 reads a[1], ...
c[idx] = a[idx] + b[idx];

// BAD: strided — thread 0 reads a[0], thread 1 reads a[stride], ...
c[idx] = a[idx * stride] + b[idx * stride];  // stride > 1 wastes bandwidth
```

- Для деформации из 32 потоков объединенный доступ загружает 128 байт (32 × 4 байта для float32) за одну транзакцию. Пошаговый доступ требует нескольких транзакций, каждая из которых загружает 128 байт, но использует только часть. Шаг, равный 32, является наихудшим случаем: каждая транзакция загружает 128 байт, но только один поток использует 4 байта (загрузка 3%).

### Общая память и тайлинг

- **Шаблон мозаики** — наиболее важный метод оптимизации графического процессора. Идея: загрузить блок данных из медленной глобальной памяти в быструю разделяемую память, выполнить на нем вычисления, а затем записать результаты обратно.

```cpp
// Matrix multiply with shared memory tiling (simplified)
__global__ void matmul_tiled(const float* A, const float* B, float* C,
                              int M, int N, int K) {
    // Shared memory for one tile of A and one tile of B
    __shared__ float tile_A[TILE_SIZE][TILE_SIZE];
    __shared__ float tile_B[TILE_SIZE][TILE_SIZE];

    int row = blockIdx.y * TILE_SIZE + threadIdx.y;
    int col = blockIdx.x * TILE_SIZE + threadIdx.x;
    float sum = 0.0f;

    // Loop over tiles
    for (int t = 0; t < (K + TILE_SIZE - 1) / TILE_SIZE; t++) {
        // Load one tile of A and B into shared memory
        if (row < M && t * TILE_SIZE + threadIdx.x < K)
            tile_A[threadIdx.y][threadIdx.x] = A[row * K + t * TILE_SIZE + threadIdx.x];
        else
            tile_A[threadIdx.y][threadIdx.x] = 0.0f;

        if (col < N && t * TILE_SIZE + threadIdx.y < K)
            tile_B[threadIdx.y][threadIdx.x] = B[(t * TILE_SIZE + threadIdx.y) * N + col];
        else
            tile_B[threadIdx.y][threadIdx.x] = 0.0f;

        __syncthreads();  // wait for all threads to finish loading

        // Compute partial dot product from this tile
        for (int k = 0; k < TILE_SIZE; k++) {
            sum += tile_A[threadIdx.y][k] * tile_B[k][threadIdx.x];
        }

        __syncthreads();  // wait before loading the next tile
    }

    if (row < M && col < N)
        C[row * N + col] = sum;
}
```

- **`__shared__`**: объявляет память, совместно используемую всеми потоками в блоке (быстрая, встроенная).
- **`__syncthreads()`**: барьер, ожидающий, пока все потоки в блоке не достигнут этой точки. Требуется между записью в общую память и чтением из нее (в противном случае некоторые потоки будут читать устаревшие данные).
- **Почему тайлинг работает**: без него каждый поток загружается из глобальной памяти при каждом умножении. При тайлинге блок данных TILE_SIZE × TILE_SIZE загружается один раз в общую память и повторно используется всеми потоками в блоке. Коэффициент повторного использования равен TILE_SIZE, что уменьшает трафик глобальной памяти на этот коэффициент.

## Потоки и параллелизм

— По умолчанию операции CUDA являются последовательными: ЦП запускает ядро, ждет его завершения, затем запускает следующее. **Потоки** включают перекрытие:

```cpp
cudaStream_t stream1, stream2;
cudaStreamCreate(&stream1);
cudaStreamCreate(&stream2);

// These operations can overlap: different streams execute concurrently
cudaMemcpyAsync(d_a, h_a, bytes, cudaMemcpyHostToDevice, stream1);
cudaMemcpyAsync(d_b, h_b, bytes, cudaMemcpyHostToDevice, stream2);

kernel1<<<grid, block, 0, stream1>>>(d_a, d_c);
kernel2<<<grid, block, 0, stream2>>>(d_b, d_d);
```

— Потоки перекрывают передачу данных с вычислениями: пока работает ядро одного потока, другой поток копирует данные. Это скрывает задержку передачи PCIe и сохраняет занятость графического процессора.

## Профилирование кода CUDA

```bash
# NVIDIA Nsight Compute: kernel-level profiling
ncu --set full ./my_program

# NVIDIA Nsight Systems: system-level timeline
nsys profile ./my_program

# Quick metrics
ncu --metrics sm__throughput,dram__throughput ./my_program
```

- **На что обратить внимание**:
    - **Занятость**: используемая доля мощности SM. Низкая заполняемость (< 50%) means too few threads to hide memory latency. Causes: too many registers per thread, too much shared memory per block.
    - **Memory throughput**: compare to peak bandwidth. If you achieve < 50% of peak, memory access patterns are inefficient (non-coalesced, bank conflicts).
    - **Compute throughput**: compare to peak FLOPS. If both memory and compute throughput are low, the kernel is latency-bound (not enough parallelism).

## Advanced Optimisation Techniques

- Beyond the basics of coalescing and shared memory tiling, high-performance GPU (and CPU) code uses several advanced techniques:

### Data Layout: AoS vs SoA

- **Array of Structures (AoS)**: each element stores all its fields together. `[{x,y,z}, {x,y,z}, {x,y,z}]`.
- **Structure of Arrays (SoA)**: each field is stored in its own contiguous array. `{[x,x,x], [y,y,y], [z,z,z]}`.

```cpp
// AoS: BAD for SIMD/GPU (accessing all x values touches non-contiguous memory)
struct Particle { float x, y, z, mass; };
Particle particles[N];
// particles[0].x, particles[1].x are 16 bytes apart

// SoA: GOOD for SIMD/GPU (all x values are contiguous)
struct Particles {
    float x[N], y[N], z[N], mass[N];
};
// x[0], x[1] are 4 bytes apart — perfect for coalesced access and SIMD
```

- SoA is almost always faster for data-parallel workloads (SIMD, GPU). AoS is better when you always access all fields of one element together (rare in numerical code). PyTorch tensors are SoA by nature: each feature is a contiguous dimension.

### Software Prefetching

- The CPU can be told to start loading data before it is needed, hiding memory latency:

```cpp
#include <xmmintrin.h> // для _mm_prefetch

for (int i = 0; я < n; i += 4) {
    _mm_prefetch((char*)(a + i + 64), _MM_HINT_T0);  // prefetch 64 elements ahead
    // process a[i:i+4] with SIMD
    __m128 va = _mm_load_ps(a + i);
    // ...
}
```

- The prefetch instruction is a hint: if the data is already in cache, it is a no-op. If not, the CPU starts fetching it in the background while executing other instructions. The prefetch distance (64 elements ahead in this example) should be tuned to match the memory latency and loop iteration time.

### Kernel Fusion

- **Kernel fusion** combines multiple operations into a single kernel to avoid writing intermediate results to memory. This is the single most impactful GPU optimisation for ML:

```
// UNFUSED: 3 kernel launches, 3 global memory round-trips
y = matmul(x, W)     // write y to global memory
z = y + bias          // read y, write z
out = relu(z)         // read z, write out

// FUSED: 1 kernel launch, 1 global memory write
out = fused_matmul_bias_relu(x, W, bias)  // y and z never leave SRAM
```

- For memory-bound operations (bias add, ReLU, layer norm), the memory traffic dominates execution time. Fusing eliminates the traffic entirely. PyTorch's `torch.compile` and Triton enable fusion automatically or with minimal effort.

### Mixed-Precision Kernels

- Using lower precision (FP16, BF16, INT8) for computation and higher precision (FP32) for accumulation gives the best of both worlds:

```cpp
// Tensor Core: multiply FP16 matrices, accumulate in FP32
// Each Tensor Core instruction: D (FP32) = A (FP16) × B (FP16) + C (FP32)
nvcuda::wmma::mma_sync(c_frag, a_frag, b_frag, c_frag);
```

- FP16 is 2x smaller than FP32, so it doubles memory bandwidth (the usual bottleneck) and fits 2x more data in cache. Tensor Cores process FP16 at 8-16x the rate of FP32 CUDA cores. This is why mixed-precision training (chapter 6) provides 2-3x speedup with minimal accuracy loss.

### Memory Pool Allocators

- `cudaMalloc` is slow (~1 ms per call) because it synchronises with the GPU. In a training loop that allocates temporary buffers every iteration, this adds up.

- **Memory pools** (PyTorch's caching allocator, CUDA memory pools) pre-allocate a large block of GPU memory and sub-allocate from it without system calls:

```python
# PyTorch does this automatically — but understanding why matters
# Each torch.empty() reuses memory from the pool, no cudaMalloc
temp = torch.empty(1024, 1024, device='cuda')  # microseconds, not milliseconds
```

- This is why PyTorch's `torch.cuda.memory_allocated()` and `torch.cuda.max_memory_allocated()` differ: allocated is what is currently in use, max is the peak (the pool may hold more than is currently used).

### Profile-Guided Optimisation

- Do not optimise blindly. **Profile first**, identify the bottleneck, optimise that, and re-profile. The roofline model (file 01) tells you whether the bottleneck is memory or compute:

    - **Memory-bound** (low arithmetic intensity): optimise data layout (SoA), fuse kernels, use lower precision, prefetch.
    - **Compute-bound** (high arithmetic intensity): use Tensor Cores, increase parallelism, use faster instructions (FMA).
    - **Latency-bound** (insufficient parallelism): increase occupancy, reduce register usage, launch more threads.

- Most ML workloads are **memory-bound**. The surprising implication: a faster GPU (more FLOPS) often does not help. Faster memory (HBM3 vs HBM2e) helps more. This is why the A100→H100 upgrade is not just about FLOPS — the H100 also has 2x the memory bandwidth.

## NVIDIA GPU Generations

| Generation | Year | Key Innovation | AI Relevance |
|------------|------|----------------|--------------|
| Pascal (P100) | 2016 | HBM2, NVLink | First serious deep learning GPU |
| Volta (V100) | 2017 | **Tensor Cores** (mixed-precision matmul) | Enabled FP16 training, 125 TFLOPS TF32 |
| Ampere (A100) | 2020 | TF32, Sparsity, 3rd gen Tensor Cores | 312 TFLOPS TF32, structural sparsity 2:4 |
| Hopper (H100) | 2022 | **Transformer Engine** (FP8), HBM3 | 989 TFLOPS FP8, dynamic precision switching |
| Blackwell (B200) | 2024 | 2nd gen Transformer Engine, NVLink 5 | 2.5 PFLOPS FP4, multi-die design |

- **Tensor Cores** are specialised matrix multiply units. A single Tensor Core instruction computes a 4×4 matrix multiply (D = A×B + C) in one cycle. Regular CUDA cores would need 64 FMA operations. Tensor Cores are why mixed-precision training (float16 compute, float32 accumulation) is fast.

- **The Transformer Engine** (Hopper+) dynamically switches between FP8 and FP16 precision within a single layer, choosing higher precision only where needed. This maximises throughput without sacrificing model quality. It is specifically designed for transformer architectures (attention + MLP), which dominate modern AI.

## Coding Tasks (compile with nvcc)

1. Write a CUDA kernel that applies ReLU to an array. Measure the time including memory transfers. This teaches kernel writing, cudaMalloc/cudaMemcpy, and the host↔device transfer bottleneck.
```cpp
// task1_relu.cu
// Compile: nvcc -O3 -o task1_relu task1_relu.cu

#include <stdio.h>
#include <stdlib.h>
#include <cuda_runtime.h>

__global__ void relu_kernel(const float* input, float* output, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    если (idx < n) {
        output[idx] = input[idx] > 0.0f ? input[idx] : 0.0f;
    }
}

интервал основной() {
    const int N = 1 << 24;  // ~16M elements
    size_t bytes = N * sizeof(float);

    // Allocate host memory
    float* h_input  = (float*)malloc(bytes);
    float* h_output = (float*)malloc(bytes);
    for (int i = 0; i < N; i++) {
        h_input[i] = (float)(i % 100) - 50.0f;  // mix of positive and negative
    }

    // Allocate device memory
    float *d_input, *d_output;
    cudaMalloc(&d_input, bytes);
    cudaMalloc(&d_output, bytes);

    // Time the full pipeline: copy to GPU, compute, copy back
    cudaEvent_t start, stop;
    cudaEventCreate(&start);
    cudaEventCreate(&stop);

    cudaEventRecord(start);
    cudaMemcpy(d_input, h_input, bytes, cudaMemcpyHostToDevice);

    int block_size = 256;
    int grid_size = (N + block_size - 1) / block_size;
    relu_kernel<<<grid_size, block_size>>>(d_input, d_output, N);

    cudaMemcpy (h_output, d_output, байты, cudaMemcpyDeviceToHost);
    cudaEventRecord (стоп);
    cudaEventSynchronize (стоп);

    плавающее мс = 0;
    cudaEventElapsedTime(&ms, start, stop);

    // Проверка
    int ошибки = 0;
    for (int i = 0; я < N; i++) {
        float expected = h_input[i] > 0.0f ? h_input[i]: 0.0f;
        if (h_output[i] != ожидается) ошибки++;
    }

    printf("Время (включая перемещения): %.2f мс\n", мс);
    printf("Пропускная способность: %.1f ГБ/с\n", 2,0 * байт/мс/1e6);  // чтение + запись
    printf("Ошибки: %d / %d\n", ошибки, N);cudaFree (d_input); cudaFree (d_output);
    бесплатно (h_input); бесплатно (h_output);
    вернуть 0;
}
```

2. Write a tiled matrix multiply in CUDA using shared memory. Compare the performance against a naive (non-tiled) version. This teaches shared memory, `__syncthreads`, and why tiling matters.
```cpp
// Task2_matmul.cu
// Компиляция: nvcc -O3 -o Task2_matmul Task2_matmul.cu

#include <stdio.h>
#include <cuda_runtime.h>

#define TILE 16

// Наивный matmul: каждый поток вычисляет один элемент C
__global__ void matmul_naive(const float* A, const float* B, float* C, int N) {
    int row = blockIdx.y * blockDim.y + threadIdx.y;
    int col = blockIdx.x * blockDim.x + threadIdx.x;
    если (строка < N && col < N) {
        float sum = 0.0f;
        for (int k = 0; k < N; k++) {
            sum += A[row * N + k] * B[k * N + col];
        }
        C[row * N + col] = sum;
    }
}

// Tiled matmul: use shared memory to reduce global memory accesses
__global__ void matmul_tiled(const float* A, const float* B, float* C, int N) {
    __shared__ float sA[TILE][TILE];
    __shared__ float sB[TILE][TILE];

    int row = blockIdx.y * TILE + threadIdx.y;
    int col = blockIdx.x * TILE + threadIdx.x;
    float sum = 0.0f;

    for (int t = 0; t < (N + TILE - 1) / TILE; t++) {
        sA[threadIdx.y][threadIdx.x] = (row < N && t*TILE+threadIdx.x < N)
            ? A[row * N + t*TILE + threadIdx.x] : 0.0f;
        sB[threadIdx.y][threadIdx.x] = (t*TILE+threadIdx.y < N && col < N)
            ? B[(t*TILE + threadIdx.y) * N + col] : 0.0f;

        __syncthreads();
        for (int k = 0; k < TILE; k++)
            sum += sA[threadIdx.y][k] * sB[k][threadIdx.x];
        __syncthreads();
    }

    if (row < N && col < N)
        C[row * N + col] = sum;
}

int main() {
    const int N = 1024;
    size_t bytes = N * N * sizeof(float);

    float *d_A, *d_B, *d_C;
    cudaMalloc(&d_A, bytes); cudaMalloc(&d_B, bytes); cudaMalloc(&d_C, bytes);

    // Initialise with ones (easy to verify: C should be all N)
    float* h_A = new float[N*N];
    for (int i = 0; i < N*N; i++) h_A[i] = 1.0f;
    cudaMemcpy(d_A, h_A, bytes, cudaMemcpyHostToDevice);
    cudaMemcpy(d_B, h_A, bytes, cudaMemcpyHostToDevice);

    dim3 block(TILE, TILE);
    dim3 grid((N+TILE-1)/TILE, (N+TILE-1)/TILE);

    // Benchmark naive
    cudaEvent_t start, stop;
    cudaEventCreate(&start); cudaEventCreate(&stop);

    cudaEventRecord(start);
    for (int i = 0; i < 10; i++)
        matmul_naive<<<grid, block>>>(d_A, d_B, d_C, N);
    cudaEventRecord (стоп);
    cudaEventSynchronize (стоп);
    плавать naive_ms; cudaEventElapsedTime(&naive_ms, старт, стоп);

    // Тестовый мозаичный тест
    cudaEventRecord (начало);
    for (int i = 0; я < 10; i++)
        matmul_tiled<<<grid, block>>>(d_A, d_B, d_C, N);
    cudaEventRecord (стоп);
    cudaEventSynchronize (стоп);
    плавать tiled_ms; cudaEventElapsedTime(&tiled_ms, start, stop);

    двойной gflops_naive = 2,0 * N * N * N * 10/naive_ms/1e6;
    двойной gflops_tiled = 2,0 * N * N * N * 10 / tiled_ms / 1e6;

    printf("Наивно: %.2f мс, %.1f GFLOPS\n", naive_ms/10, gflops_naive);
    printf("Плитка: %.2f мс, %.1f GFLOPS\n", tiled_ms/10, gflops_tiled);
    printf("Ускорение: %.1fx\n", naive_ms / tiled_ms);

    cudaFree(d_A); cudaFree(d_B); cudaFree(d_C);
    удалить [] h_A;
    вернуть 0;
}
```

3. Demonstrate warp divergence. Write a kernel where threads in the same warp take different branches, and compare against a branchless version.
```cpp
// Task3_divergence.cu
// Компиляция: nvcc -O3 -o Task3_diverge Task3_divergence.cu

#include <stdio.h>
#include <cuda_runtime.h>

// ПЛОХО: расхождение варпов — четные/нечетные потоки идут разными путями
__global__ void divergent_kernel(float* data, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    если (idx < n) {
        if (idx % 2 == 0) {
            data[idx] = data[idx] * 2.0f + 1.0f;
        } else {
            data[idx] = data[idx] * 0.5f - 1.0f;
        }
    }
}

// GOOD: branchless — all threads execute the same instruction
__global__ void branchless_kernel(float* data, int n) {
    int idx = blockIdx.x * blockDim.x + threadIdx.x;
    if (idx < n) {
        float scale = (idx % 2 == 0) ? 2.0f : 0.5f;
        float offset = (idx % 2 == 0) ? 1.0f : -1.0f;
        data[idx] = data[idx] * scale + offset;
    }
}

int main() {
    const int N = 1 << 24;
    float* d_data;
    cudaMalloc(&d_data, N * sizeof(float));
    cudaMemset(d_data, 0, N * sizeof(float));

    int block = 256, grid = (N + block - 1) / block;

    cudaEvent_t start, stop;
    cudaEventCreate(&start); cudaEventCreate(&stop);

    // Divergent
    cudaEventRecord(start);
    for (int i = 0; i < 100; i++)
        divergent_kernel<<<grid, block>>>(d_data, N);
    cudaEventRecord (стоп);
    cudaEventSynchronize (стоп);
    плавающий div_ms; cudaEventElapsedTime(&div_ms, старт, стоп);

    // Без филиалов
    cudaEventRecord (начало);
    for (int i = 0; я < 100; i++)
        branchless_kernel<<<grid, block>>>(d_data, N);
    cudaEventRecord (стоп);
    cudaEventSynchronize (стоп);
    плавать nodiv_ms; cudaEventElapsedTime(&nodiv_ms, старт, стоп);

    printf("Дивергент: %.2f ms\n", div_ms / 100);
    printf("Без ветвей: %.2f ms\n", nodiv_ms / 100);
    printf("Ускорение: %.2fx\n", div_ms / nodiv_ms);

    cudaFree (d_data);
    вернуть 0;
}
```